import { Box, makeStyles } from "@material-ui/core";

const useStyles = makeStyles({});
export default function OrderCard() {
  const classes = useStyles();
  return <Box className={classes.orderCardContainer}>
      
  </Box>;
}
